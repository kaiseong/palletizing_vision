# 프로젝트 목표 — RB-Y1 시각 기반 Box Pick 및 인터락 팔레타이징

> 기준 코드: `7e581b3` 이후의 slot-1 배치 개정. 배치 동작은 `Box_placing/docs/adr/0008-base-y-release-without-descent.md`가 최종 권위다.
>
> 이 문서는 새로운 Agent가 과거 대화를 다시 읽지 않고도 프로젝트의 최종 목표, 물리 조건, 현재 구현 수준, 안전 제약, 다음 구현 순서를 이해하도록 만드는 인수인계 문서다.
>
> **갱신 이력**
> - 2026-07-30: 8.6절 배치 동작을 ADR 0008(수직 하강 제거 → base Y 개방)로 교체. 실측 gap 159 mm를 15절에 반영. 14절에 슬림화 문서 추가.

## 1. 우리의 목표는 무엇인가

우리의 최종 목표는 **RB-Y1 양팔 로봇이 고정된 작업대의 택배 상자를 RGB-D 카메라로 인식해 집고, 팔레트 앞으로 이동한 뒤, 네 개의 상자로 한 층을 이루는 핀휠(pinwheel) 인터락 구조에 맞춰 반복 적재하는 것**이다.

완성 시스템의 한 사이클은 다음과 같아야 한다.

```text
상자 관측
  → 상자 중심 x/y/z 및 장축 yaw 추정
  → 모바일 베이스 x/y/yaw 동시 정렬
  → 양팔 파지 및 리프트
  → 팔레트 앞까지 운반
  → 적층 좌표계와 현재 슬롯 추정
  → 모바일 베이스 x/y/yaw 동시 정렬
  → 슬롯 종류에 맞는 직접 배치 또는 한 손 슬라이드 배치
  → 손 이탈 및 안전 후퇴
  → 다음 상자와 다음 슬롯으로 반복
```

이 목표는 두 단계로 나뉜다.

| 단계 | 목표 | 현재 상태 |
| --- | --- | --- |
| `box_pick` | 작업대 상자의 pose를 추정하고, 베이스 정렬 후 양팔로 집어서 들어 올림 | **구현됨. 사용자 실기 관찰상 동작함**. 다만 고정 가로 파지와 nominal calibration에 한정됨 |
| `palletizing` | 집은 상자를 인터락 적층의 올바른 층·슬롯에 정렬하고 놓음 | **3층 1번 슬롯 정렬/배치 MVP만 구현됨**. 전체 인터락 적재는 미완성 |

따라서 “팔레타이징이 구현됐다”라고 표현하면 안 된다. 현재 정확한 표현은 다음과 같다.

> Box pick은 데모 가능한 상태이고, 팔레타이징은 **상자를 든 상태에서 3층 1번 슬롯 위로 정렬하고 제한적으로 하강·release하는 감독형 MVP를 커미셔닝 중**이다.

---

## 2. 상태 표기 규칙

이 문서에서는 다음 상태를 구분한다.

- **구현됨**: 기준 커밋에 코드가 존재하고 해당 경로가 실행된다.
- **사용자 실기 관찰됨**: 사용자 제공 로그 또는 이전 실제 RB-Y1 실행에서 의도한 동작이 관찰됐다. 독립적인 위치 측정이나 반복 성공 검증을 뜻하지 않는다.
- **커미셔닝 중**: 코드는 존재하지만 반복 성공성과 물리 정확도가 입증되지 않았다.
- **목표 설계**: 사용자와 합의한 최종 방향이나 모션 개념이지만 아직 코드로 완성되지 않았다.
- **미결정**: 실측 또는 추가 합의가 필요하며 Agent가 임의로 값을 정하면 안 된다.

---

## 3. 고정된 시스템 조건

### 3.1 로봇과 실행 환경

| 항목 | 값 |
| --- | --- |
| 로봇 | Rainbow Robotics RB-Y1 |
| 모델 / 버전 | Model M / v1.2 |
| 구성 | 양팔, torso, head, mobile base |
| Robot PC | NVIDIA Jetson AGX Orin |
| JetPack | 6.2.2 |
| Python | 3.12 |
| 카메라 | Intel RealSense D435 |
| Pallet 설정의 카메라 serial | `250122079439` |
| RGB | 640×480, 30 FPS, BGR8 |
| Depth | 640×480, 30 FPS, Z16 |
| 영상 정합 | RGB를 Depth 픽셀 격자에 정합 |

Python 3.12 호환성을 유지해야 한다. Robot PC에 없는 신규 의존성을 쉽게 추가하지 말고, 현재의 NumPy/OpenCV/RealSense/RB-Y1 SDK 경로를 우선 사용한다.

### 3.2 카메라와 고정 설치

- 카메라는 head에 고정되어 있다.
- 카메라, head, torso, 책상 높이와 자세가 동일해야 현재 calibration을 사용할 수 있다.
- 이 중 하나라도 움직였다면 빈 책상 데이터로 table plane과 고정 registration을 다시 검증해야 한다.
- 사용자 제공 장착 seed는 `head_link_2 → RGB camera` 기준 다음 값이다.

```text
[x, y, z, roll, pitch, yaw]
[0.049, -0.0115, 0.057, -90°, 0°, -90°]
Euler order: ZYX
```

- D435 데이터시트의 `Camera Front Glass / Depth Start Point = -4.2 mm`는 **기계 도면상의 기준점**이다.
- 이 `-4.2 mm`를 RealSense SDK의 depth 값이나 camera transform에 다시 더하면 안 된다.

### 3.3 상자 실측값

주문 표기 치수와 실제 상자 치수가 달라, 다음 8개를 직접 측정했다.

```text
400×253×160 mm
395×252×164 mm
395×254×164 mm
400×256×161 mm
401×252×159 mm
401×255×156 mm
399×253×160 mm
400×253×159 mm
```

현재 estimator와 pallet 설정에서 사용하는 고정 prior는 다음과 같다.

```text
대표 크기: 400×253×160 mm
관측 범위: 395–401 × 252–256 × 156–164 mm
```

상자는 좌우·상하가 대칭이므로 yaw는 방향 벡터가 아니라 **180° 주기의 직선 방향**이다. 즉 `+90°`와 `-90°`는 같은 장축 방향이다.

사람에게 표시할 때는 대략 다음 두 계열로 볼 수 있지만, 내부 제어는 연속 yaw를 사용해야 한다.

- `-45° ~ +45°`: 0° 계열
- `46° ~ 134°` 및 부호 seam 부근: 90° 계열

### 3.4 팔레트 적층 실측값

네 상자가 한 층의 핀휠 링을 이룬 상태에서 직접 측정한 값이다.

```text
한 층 외곽: 660×658 mm
중앙 빈 공간: 148×149 mm
```

이 값은 단순 이미지 bbox 크기가 아니라 metric stack model의 고정 치수로 사용한다.

---

## 4. 좌표계와 calibration 계약

### 4.1 Base 좌표 출력

상자와 팔레트 pose는 최종적으로 RB-Y1 `base` 좌표계로 표현한다.

현재 변환 개념은 다음과 같다.

```text
T_base_from_depth
  = T_base_from_head
  × T_head_from_color
  × E_color_from_depth
```

그 후 현재 설정에는 경험적 base translation correction이 추가된다.

```text
[x, y, z] correction = [0.000, +0.050, 0.000] m
```

중요한 한계:

- 현재 registration 상태는 `nominal_unverified` 또는 부분 plane calibration 수준이다.
- `+0.050 m` y 보정은 2026-07-28 한 번의 작업자 눈대중 정렬에서 얻은 값이다.
- 독립적인 지그, fiducial ground truth, touch probe로 검증한 절대 좌표가 아니다.
- 따라서 현재 base 좌표 결과를 `absolute`, `ground-truth validated`, `정밀 보정 완료`라고 부르면 안 된다.
- Box picking operator 명령은 현재 nominal registration을 기본 승인하고 시작 시 경고를 출력한다.
- Box placing에서 실기 자동 이동을 켤 때는 기존 `--allow-nominal-registration` 승인 플래그로 이 위험을 명시적으로 승인해야 한다.

### 4.2 팔레트 로컬 축

팔레트 적층 geometry는 다음 로컬 축을 사용한다.

- `u_right`: 카메라 영상에서 로봇이 보았을 때 오른쪽 방향. D435 depth optical `+X`를 stack plane에 투영해 만든다.
- `v_far`: 로봇에서 팔레트 안쪽, 즉 먼 방향.
- 현재 고정 접근 조건에서는 `v_far`가 RB-Y1 base `+X`와 같은 방향이 되도록 branch를 고정한다.
- stack plane normal은 위쪽을 향하도록 정규화한다.

한쪽 면에서만 접근하더라도 90° branch를 임의 선택하면 슬롯의 오른쪽/먼쪽 의미가 뒤집힐 수 있으므로, 코드에서는 `image_right` branch와 `v_far → base +X` 계약을 유지한다.

---

## 5. 알고리즘의 기본 원칙

이 프로젝트는 고정된 물리 조건을 적극적으로 사용한다.

사용하는 정보:

- RGB image
- Depth image
- depth intrinsic (`fx`, `fy`, `cx`, `cy` 등)
- RGB↔Depth extrinsic
- 고정 카메라 장착 transform
- 고정 table plane
- 고정 상자 크기와 높이 범위
- 고정 팔레트 한 층 외곽과 중앙 홀 크기
- 양팔 EEF FK

기본 원칙:

1. 최종 metric pose의 주 근거는 RGB bbox가 아니라 **Depth + calibration + metric plane geometry**다.
2. 상자 pick에는 calibrated table/top plane과 고정 크기 직사각형 fitting을 사용한다.
3. 팔레트에는 stack top plane과 고정 660×658 mm 외곽 / 148×149 mm 중앙 홀 모델을 사용한다.
4. RGB는 정합, 시각화, 보조 분리용이지 최종 거리 scale의 단독 근거가 아니다.
5. Depth가 시끄럽더라도 버리지 않고 plane discovery, height gating, metric consistency와 confidence에 사용한다.
6. 영상 crop, 가림, edge 부족으로 자유도가 관측 불가능하면 강제로 pose를 만들지 말고 invalid/hold로 처리한다.
7. 시각화 overlay가 실제 물체와 맞지 않으면 threshold부터 만지기 전에 plane, 치수, 좌표변환, frame 정의가 맞는지 먼저 점검한다.

---

## 6. 이미 구현한 Box Pick

### 6.1 Box Pick의 목적

현재 box-pick은 테이블 위 단일 상자를 인식해 상자 중심과 장축 yaw를 얻고, 모바일 베이스를 파지 가능한 기준 pose로 정렬한 뒤 양팔로 집어 들어 올린다.

현재 지원하는 파지 유형은 **영상에서 가로로 긴 상자, 즉 장축 yaw가 ±90° 계열인 자세**다. 0° 계열의 세로 파지는 별도의 arm ready/grasp pose가 아직 필요하다.

### 6.2 상자 pose 추정

주요 구현: `Box_picking/src/parcel_pose_picking/estimator.py`

개념 흐름:

```text
Depth + intrinsic
  → 빈 책상에서 보정한 table plane
  → table plane보다 약 160 mm 위의 top-plane slab
  → ray/plane intersection으로 metric 3D 점 복원
  → 400×253 mm 고정 직사각형 fitting
  → base 기준 상자 중심 x/y/z + 장축 yaw
```

출력에서 다음을 구분해야 한다.

- top-surface center: 상자 윗면 중심
- volume center: 상자 부피 중심
- long-axis yaw: 180° 대칭인 장축 선 방향

`x/y/z와 yaw를 뽑는다`는 요구에서 z는 단순 depth optical Z가 아니라 base frame의 metric 높이다.

### 6.3 모바일 정렬 목표

현재 가로 파지의 목표는 corrected RB-Y1 base 좌표에서 다음과 같다.

```text
상자 중심 target: x = 0.740 m, y = 0.000 m
상자 장축 target: yaw = +90° modulo 180°
```

제어는 x를 맞춘 뒤 y를 맞추는 순차 jogging이 아니다. 하나의 20 Hz SE(2) mobile stream에서 `vx`, `vy`, `wz`를 **동시에** 계산해 연속 정렬한다.

상자 pose가 stale하거나 jump가 크거나 관측이 사라지면 nonzero command를 내리지 않는다. 도착 판정은 여러 프레임과 시간 조건을 만족한 뒤 exact-zero command와 wheel-stop 확인으로 끝난다.

### 6.4 Box Pick 자세와 동작 순서

카메라 calibration 자세:

```text
Torso [deg]: [0, 55, -59.988, 6.532, 0, 0]
Head  [deg]: [0, 49.846]
```

베이스 이동 전 arm-only mobile-ready 자세:

```text
Right arm [deg]: [6.644, -21.489, -17.252, -129.031, -83.302, 53.394, 37.071]
Left arm  [deg]: [6.644,  21.488,  17.245, -129.036,  83.304, 53.392, -37.070]
```

이 command는 torso/head를 바꾸지 않는다. 현재 구현 값은 minimum time `0.2 s`, hold `0.01 s`, timeout `10 s`인 arm-only Joint Position one-shot이다.

실제 grab 시작 자세는 radians 기준 다음과 같다.

```text
Torso    : [ 0.000,  0.960, -1.047,  0.114,  0.000,  0.000]
Right arm: [-0.375, -0.391, -0.238, -1.576, -1.013,  1.467, -0.003]
Left arm : [-0.375,  0.391,  0.238, -1.576,  1.013,  1.467,  0.003]
Head     : [ 0.000,  0.870]
```

전체 순서:

```text
1. RGB-D로 상자 pose 추정
2. 필요하면 torso/head를 카메라 calibration 자세로 5 s Position one-shot 복원
3. 양팔을 mobile-ready 자세로 arm-only Position one-shot
4. 모바일 베이스로 x/y/yaw 동시 정렬
5. exact zero + wheel stop 확인
6. grab 시작 자세로 Joint Position 이동
7. 양팔 Cartesian Impedance로 상자를 y축 방향에서 파지
8. 파지 힘을 유지한 채 양 EEF local +z로 들어 올림
```

파지/리프트의 현재 주요 상수:

| 항목 | 현재 값 |
| --- | --- |
| 가상 grab inward offset | 손마다 `0.300 m` |
| grab translational stiffness | `[1000, 200, 1000]` |
| grab rotational stiffness | `[50, 50, 50]` |
| grab hold | `0.5 s` |
| lift | 각 EEF local `+z`로 `0.150 m` |
| lift 중 inward squeeze | `0.150 m` |
| lift joint stiffness | 양팔 각 joint `150 Nm/rad` |
| torque limit | SDK default |
| lift hold | `100 s` |

`GRAB_DISTANCE=0.300 m`와 `LIFT_SQUEEZE_DISTANCE=0.150 m`는 손이 실제로 그 거리만큼 관통 이동한다는 뜻이 아니라, Cartesian impedance가 상자 쪽으로 계속 힘을 내도록 만든 target error다.

### 6.5 Box Pick의 현재 한계

- 고정 카메라·책상·상자 크기 조건에 의존한다.
- 가로 파지(장축 ±90°)만 현재 자세가 준비되어 있다.
- calibration은 nominal이며 독립 ground truth가 없다.
- `FTMonitor`는 존재하지만 force/torque abort threshold 기본값이 `None`이다.
- 100 s lift hold는 팔레타이징으로 원자적으로 handoff하는 완성 경로가 아니라 데모용 유지 구간이다.
- box-pick runtime과 pallet runtime 사이의 단일 stream ownership handoff는 아직 완성되지 않았다.

---

## 7. 구현해야 하는 인터락 팔레타이징

### 7.1 최종 적층 구조

한 층은 네 상자가 중앙의 약 148×149 mm 공간을 둘러싸는 핀휠 링이다. 인접 층은 방향을 반대로 구성해 상자 이음새가 겹치지 않는 인터락 구조를 만든다.

현재 작업의 시작점은 **이미 두 층이 쌓인 상태에서 3층을 만드는 것**이다.

3층의 사용자 의도:

| 순서 | 상자 방향/위치 | 배치 방식 | 핵심 이유 |
| --- | --- | --- | --- |
| 1번 | 먼쪽 가로 | 직접 배치 | 주변 손 이탈 공간이 있어 가장 단순함. 현재 MVP 대상 |
| 2번 | 1번 옆의 먼쪽 세로 | 바깥쪽 offset 후 한 손 slide | 목표에 바로 내리면 안쪽 손이 1번 상자 사이에 갇힘 |
| 3번 | 가까운쪽 세로 | 직접 배치 | 중앙 홀과 아직 빈 슬롯 쪽으로 손 이탈 공간 확보 가능 |
| 4번 | 가까운쪽 가로 | 바깥쪽 offset 후 한 손 slide | 링을 닫는 마지막 상자라 손 끼임과 누적 오차가 가장 큼 |

다음 층은 인터락을 위해 좌우가 반대로 된다. 따라서 하드코딩된 8개의 독립 모션보다 최종적으로 다음 네 primitive와 layer parity를 조합하는 구조가 적절하다.

```text
DIRECT_FAR
SLIDE_FAR
DIRECT_NEAR
SLIDE_NEAR
```

layer parity가 바꾸는 값:

- 슬롯의 좌우 방향
- 갇히는 inner hand
- pre-place offset 방향
- 한 손 slide 방향
- 상자 장축 방향

단, 이 primitive 구조는 **목표 설계**이며 현재 코드에는 slot 2~4와 layer parity가 구현되어 있지 않다.

### 7.2 왜 모바일 베이스와 팔의 역할을 나누는가

권장 역할:

| 제어기 | 역할 |
| --- | --- |
| Mobile base | 큰 x/y/yaw 오차를 coupled visual servo로 정렬 |
| 양팔 Cartesian Impedance | 최종 수직 하강, 하중 전달, release, 한 손 push/slide |
| 비전 | stack frame, slot error, held-box와 stack 사이 clearance, 배치 후 결과 검증 |

베이스는 최종 접촉이나 slide 중에는 exact zero로 고정해야 한다. mecanum slip, base rotation, arm impedance가 동시에 상자 접촉력에 관여하면 거동과 충돌 위험을 예측하기 어렵다.

### 7.3 팔레트 인식 전략

외부 marker 기반 orchestrator가 로봇을 팔레트 정면에 대략 정렬해 주는 것을 전제로 한다. 하지만 marker는 팔레트 옆면에 있고, 1·2번 슬롯을 놓기 위해 가까이 접근하면 head camera에서 보이지 않을 가능성이 높다.

따라서 close-range 정렬의 주 근거는 head D435의 stack geometry다.

인식 우선순위:

1. stack top plane을 metric depth로 찾는다.
2. 중앙 148×149 mm hole이 완전히 보이면 center와 두 축을 직접 fitting한다.
3. 멀어서 hole이 안 보이면, 보이는 가까운 외곽 edge와 영상 오른쪽 side edge가 만드는 L-corner를 660×658 mm 고정 외곽 모델에 맞춘다.
4. 이 L-corner에서 stack center와 중앙 hole proxy를 복원한다.
5. 가까워져 complete hole이 보이면 같은 stack frame을 refine/cross-check한다.

즉 “중앙 홀을 볼 때까지 무조건 전진”도 아니고 “홀 하나만 계속 추적”도 아니다. 외곽 L-corner는 coarse acquisition과 close-range fallback을 제공하고, complete hole은 더 직접적인 refinement evidence다.

Coarse edge evidence가 허용할 수 있는 동작은 **작은 전진만**이다. lateral/yaw/reverse/descent/release 권한을 주면 안 된다.

---

## 8. 현재 구현된 Pallet Slot-1 MVP

### 8.1 시작 전제

현재 `box_pallet.py live`는 완성된 box-pick의 즉시 후속 단계가 아니라 독립 실행이다. 실행 시작 시 다음 조건을 가정한다.

- 로봇은 upstream marker/orchestrator 등에 의해 팔레트 정면에 대략 정렬돼 있다.
- 상자는 이미 양팔에 잡혀 있다.
- 필요하면 프로그램이 slot-1 ready 자세로 먼저 이동한다.
- 현재 target은 3층 1번, 즉 먼쪽 가로 상자의 hover/place 위치다.

### 8.2 Slot-1 ready 자세

단위는 radians다.

```text
Torso    : [ 0.000,  1.410, -1.514,  0.6295,  0.000,  0.000]
Right arm: [-1.120, -0.383, -0.048, -1.066, -0.824,  1.109,  0.157]
Left arm : [-1.120,  0.383,  0.048, -1.066,  0.824,  1.109, -0.157]
Head     : [ 0.000,  0.870]
```

현재 동작:

- 모든 target joint가 1° 이내면 Position command를 건너뛴다.
- 아니면 torso, 양팔, head를 한 번의 전체 Joint Position command로 보낸다.
- minimum time은 `5.0 s`다.
- 완료 응답 후 실제 joint pose를 다시 검증한다.

### 8.3 Slot-1 metric target

설정 값:

```text
outer size:  [0.660, 0.658] m
opening:     [0.148, 0.149] m
slot1 offset from opening proxy:
  +0.12800 m along u_right
  +0.20175 m along v_far
slot1 long axis: u_right
```

현재 operator-demonstrated fine reference:

```text
opening/proxy center in base: [0.865000, 0.139523] m
yaw in base: -90.0°
source: pallet_slot1 complete-hole dwell 16 frames
status: nominal_unverified_operator_demonstrated
```

이 reference는 현재 ready/grasp/camera family의 눈대중 도착점을 재현하기 위한 값이지 절대 팔레트 ground truth가 아니다.

현재 held-box pose도 완전한 hand–box calibration이 아니라 fixed ready 자세의 EEF midpoint에 경험적 offset을 더해 사용한다.

```text
held-box center offset from EEF midpoint:
  [0.085463, 0.008236, 0.000000] m
frame:
  base at fixed pallet-ready pose
status:
  nominal_unverified_not_external_ground_truth
```

양 EEF에서 복원한 box center 불일치 허용값은 `0.015 m`, yaw 불일치 허용값은 `2°`다. 향후 실제 hand–box transform을 보정하면 이 경험적 offset을 대체해야 한다.

### 8.4 Slot-1 접근 및 정렬

현재 흐름:

```text
대략 정면 정렬 상태
  → slot-1 ready pose 확인/이동
  → stack/held-box clearance 확인
  → hole 또는 strict outer L-corner 탐색
  → 필요 시 최대 0.15 m 범위의 bounded forward acquisition
  → stationary metric feature 확보
  → x/y/yaw coupled fine servo
  → arrival dwell
  → exact-zero command acknowledgement
  → wheel stop 확인
  → ARRIVED_HOLD
```

주요 수치:

| 항목 | 값 |
| --- | --- |
| acquisition 전진 속도 | `0.030 m/s` |
| acquisition 총 budget | `0.150 m` |
| fine max linear speed | `0.050 m/s` |
| fine max angular speed | `0.080 rad/s` |
| arrival inner | `0.010 m`, `3°` |
| arrival outer | `0.015 m`, `5°` |
| arrival 유지 | 최소 5 frames 및 `0.35 s` |
| wheel stop 유지 | `0.35 s` |

사용자가 제공한 실제 실행 로그에서 slot-1 alignment-only가 `ARRIVED_HOLD`까지 도달한 적은 있다. 따라서 L-corner/hole 인식과 fine alignment 경로는 **사용자 실기 관찰됨**으로 기록한다. 이는 기준 코드만으로 재검증한 결과가 아니며, 위치 정확도의 독립 실측과 반복 성공률은 아직 없다.

### 8.5 하나의 persistent combined command stream

Pallet runtime은 다음 command를 **하나의 소유자, 하나의 persistent stream**에서 함께 보낸다.

- torso/head Position hold
- 양팔 Cartesian loaded hold / lowering / release
- mobile base SE(2)

현재 stream은 20 Hz이며 control hold time은 `1.0 s`, 최소 packet time은 `0.05 s`다.

Body stream과 mobile stream을 서로 다른 owner로 나누면 SDK priority arbitration과 stream expiry 때문에 안전한 loaded hold가 깨질 수 있다. future Agent는 편의를 위해 이를 다시 분리하면 안 된다.

### 8.6 Placement state machine

기준 커밋에는 다음 상태가 구현되어 있다.

```text
PRE_PLACE_VERIFY
  → LOWERING
  → SEATED
  → RELEASING
  → RELEASED

실패 시 → FAULT_HOLD
```

Placement 시작 조건에는 실제 fine `ARRIVED_HOLD`, 현재 metric feature, 도착 후 1.0 s loaded-arm dwell, exact-zero acknowledgement, wheel stop, fresh FK가 필요하다.

현재 설정의 배치 동작 (2026-07-30 개정, ADR 0008):

- 하강량은 `min(frozen gap × 2/3, maximum_planned_descent_m)`이며 현재
  `maximum_planned_descent_m = 0.015 m`다. 즉 **실효 하강은 15 mm**다.
  원래의 `2/3 gap`(실측 gap 159 mm에서 약 106 mm)은 양팔을 뻗게 만들어
  RB-Y1 컨트롤러가 특이점으로 명령을 거부하고 `This command stream is expired`가
  발생했다. 상한을 올리면 하강이 그대로 복귀하므로 코드 변경은 필요 없다.
- raw gap의 최대 허용값은 `0.250 m`이고, 추가로 `maximum_release_gap_m = 0.170 m`를
  넘는 gap은 `descent_gap_above_release_limit`으로 거부한다. 하강이 작아
  **낙하 높이 = gap − 15 mm**이므로 이 상한이 낙하 높이의 유일한 제어 수단이다.
- 하강 timeout은 `12 s`다.
- lower target 허용오차는 z `0.008 m`, midpoint xy drift `0.015 m`, rotation `3°`다.
- release는 동결된 `PlacementDescentPlan`의 target에서 **base `±Y` 축으로만**
  각 손을 `0.030 m` 벌린다(상한 `0.040 m`). base X와 Z는 계획 target과 비트 단위로
  동일해 수직 이동이 원리적으로 불가능하다. 실측 inter-EEF 축이 base `±Y`에서
  `10°`를 넘게 벗어나면 `release_axis_deviation`으로 fail-closed 한다.
- squeeze 상쇄는 원래부터 실제 모션이 아니었다. `loaded = measured − axis×squeeze`
  이고 release가 `+axis×(squeeze+spread)`를 더하므로 정확히 상쇄되어, 실제 주행은
  항상 손당 spread뿐이다. 특이점의 원인은 하강 단독이었다.
- release timeout은 `12 s`, target tolerance는 `0.012 m` / `4°`다.

이 경로는 **커미셔닝 중**이다. 실기에서 확인된 순서는 다음과 같다.

1. loaded Cartesian hold 중 ready-joint 오차를 `arm_tracking_error`로 오판해
   240 프레임 내내 모든 주행 명령이 zero로 치환됐다. `7e581b3`에서 수정됐고,
   현재는 회귀 테스트 20개가 이 계약을 양방향으로 고정한다.
2. 수정 후 정렬은 `ARRIVED_HOLD`, `interlock=PASS`까지 도달했다(사용자 실기 관찰됨).
3. 이어진 하강(`2/3 gap` ≈ 106 mm)에서 컨트롤러가 명령을 거부하고
   `This command stream is expired`가 발생했다. 이후 containment가 무한 루프에
   빠져 두 번째 Ctrl-C 강제 취소로 상자가 낙하했다. ADR 0008이 이를
   하강 제거·base Y 개방·containment 유한화로 대응했다.
4. `maximum_release_gap_m = 0.120`이던 시점에는 실측 gap 159 mm가
   `descent_gap_above_release_limit`으로 거부됐다. 상한을 `0.170`으로 올렸다.

**수정 후 실제 하강·release는 아직 한 번도 실기 실행되지 않았다.** 하강 15 mm에서도
특이점이 발생할지 알 수 없다. 소프트웨어 검증(테스트 87개, replay 2세션)은 물리
충돌 회피와 배치 정확도를 증명하지 않는다.

특히 다음을 오해하면 안 된다.

- 하강량 `15 mm`는 contact detection이 아니라 특이점을 피하려 상한으로 묶은
  경험적 supervised 값이다.
- F/T를 placement decision에 사용하지 않는다.
- `SEATED`와 `seating_evidence`는 **이름이 실제 동작과 어긋난다.** 접촉 검출이
  전혀 없고, 하강도 15 mm뿐이다. `SEATED`의 실제 의미는 "동결된 잔여 gap 근거가
  아직 유효하다"이며, 시작 게이트와 **같은 술어**(`gap − 불확실도 ≥ 50 mm`)를 쓴다.
  개명은 `LOWERING_MODE` / `RELEASE_MODE` 문자열 계약에 파급되므로 보류했다.
- **상자는 공중에서 놓인다.** 실측 gap 159 mm − 하강 15 mm = 약 `144 mm` 자유낙하다.
  164 mm 카톤이 그 높이에서 떨어지면 전도할 수 있다. 첫 시험에는 완충재가 필요하고,
  근본 해결은 팔레트/적재면을 높여 gap 자체를 줄이는 것이다(gap 59 mm면 하강 0으로도
  낙하 59 mm).
- vision/FK geometry가 틀리면 더 큰 공중 release 또는 과도한 하강이 가능하다.
- release spread는 손당 `0.030 m`로 줄였다(이전 `0.120 m`). 특이점 여유 확보가 목적이다.

---

## 9. 구현 상태 요약

| 기능 | 상태 | 비고 |
| --- | --- | --- |
| D435 RGB-D 640×480 동기 취득 | 구현됨 | RGB → Depth 정합 |
| 빈 책상 calibration / metric top plane | 구현됨 | 고정 setup 필요 |
| 상자 중심 x/y/z, 장축 yaw | 구현됨·사용자 실기 관찰됨 | 400×253×160 mm prior, 독립 GT 없음 |
| Box-pick base x/y/yaw coupled servo | 구현됨·사용자 실기 관찰됨 | target `(0.740, 0)`, yaw ±90° |
| 가로 상자 양팔 impedance grab/lift | 구현됨·사용자 실기 관찰됨 | 세로 파지는 미구현 |
| Pallet stack plane / hole / L-corner 인식 | 구현됨 | complete hole과 outer proxy |
| Slot-1 bounded forward acquisition | 구현됨·사용자 실기 관찰됨 | 전진만 허용 |
| Slot-1 x/y/yaw coupled fine alignment | 구현됨·사용자 로그상 ARRIVED_HOLD 관찰 | 독립 정확도 미측정 |
| Slot-1 geometry-gated lower/release | 구현됨·커미셔닝 중 | ADR 0008로 개정: 하강 `15 mm` 상한, base `±Y` 개방 `30 mm`. **개정 후 실기 실행 0회** |
| 하드웨어 없는 회귀 테스트 | 구현됨 | 87 케이스. `7e581b3` 시점에는 git 추적 테스트가 0개였다 |
| Box-pick → pallet atomic handoff | 미구현 | 100 s lift hold와 별도 runtime 상태 |
| Slot 2 offset / inner-hand escape / one-hand slide | 미구현 | 다음 핵심 난제 |
| Slot 3 direct near place | 미구현 | near-side reference 필요 |
| Slot 4 slide and ring closure | 미구현 | 가장 어려운 모션 |
| 다음 층 mirror / layer parity | 미구현 | 인터락 완성에 필수 |
| Release 후 arm/base retreat | 미구현 | 다음 상자 반복에 필수 |
| 배치 후 상자 pose 검증·교정 | 미구현 | 누적 오차 방지 필요 |
| Full layer / multi-layer 반복 | 미구현 | 프로젝트 최종 목표 |
| 절대 base calibration 검증 | 미구현 | 현재 nominal + 경험 보정 |

---

## 10. 다음 구현 순서

다음 Agent는 전체 팔레타이징을 한 번에 일반화하지 말고, 물리적으로 가장 위험한 primitive를 작은 단계로 검증해야 한다.

### 10.1 우선순위 1 — Slot-1 배치 커미셔닝 완료

1. alignment-only로 반복해서 `ARRIVED_HOLD` 수렴과 실제 위치 오차를 확인한다.
2. JSONL의 frozen gap과 실제 상자 바닥–stack top 간격을 비교한다.
3. 하강량, release spread, arm reach/singularity margin을 실측 기반으로 제한한다.
4. release 이후 상자가 안정적으로 지지되는지 확인한다.
5. arm retreat primitive를 추가한다.
6. 동일 조건 반복 성공 기준을 정의하고 충족한다.

새로운 복잡한 추상화보다 현재 slot-1 경로를 단순하고 관찰 가능하게 유지하는 것이 우선이다.

### 10.2 우선순위 2 — Slot-2 slide primitive

Slot 2는 full interlock의 핵심 검증이다.

목표 흐름:

```text
slot-2 target의 바깥쪽으로 δ offset
  → base 정지
  → 양팔로 하강 및 하중 전달
  → 갇힐 inner hand grip 해제
  → inner hand를 위/바깥쪽으로 이탈
  → outer hand를 상자 측면 중앙에 유지
  → 낮은 속도로 목표 방향 slide
  → yaw와 최종 center 검증
  → outer hand release 및 retreat
```

`δ`는 그리퍼 두께만으로 정하면 안 되고, 손을 열고 빼는 전체 swept volume으로 실측해야 한다.

한 손 push 중에는 다음을 감시해야 한다.

- 목표 상자의 yaw 회전
- 이미 놓은 1번 상자의 이동
- 아래층 상자의 이동/변형
- push 방향 이외의 xy drift
- 손의 reach와 joint margin

### 10.3 우선순위 3 — Slot 3, Slot 4, layer parity

- Slot 3의 near-side direct placement reference를 정의한다.
- Slot 4의 ring-closing slide를 먼저 별도 시험해도 된다. 3개를 미리 놓고 마지막 모션만 검증하는 것이 효율적이다.
- 다음 층에서 좌우/손/slide 방향이 반전되는 layer parity를 추가한다.
- 각 상자를 놓은 뒤 실제 pose를 다시 측정해 누적 오차를 다음 slot target에 반영한다.

### 10.4 우선순위 4 — End-to-end 통합

- box-pick lift hold에서 pallet persistent stream으로 atomic ownership handoff
- upstream marker approach와 head-camera close-range alignment의 명확한 경계
- place 후 retreat와 다음 pick 시작 자세
- failure recovery 및 operator intervention 절차
- full four-slot layer 반복과 다음 층 반복

---

## 11. 안전 및 제어 불변조건

다음은 기능보다 우선하는 규칙이다.

1. **관측 불확실 시 정지**
   stale frame, metric feature loss, 큰 center/yaw jump, 잘못된 plane, FK stale이면 nonzero mobile/placement 명령을 내리지 않는다.

2. **Coarse evidence의 권한 제한**
   relaxed outer edge는 bounded forward acquisition만 허용한다. lateral, yaw, reverse, descent, release를 허용하지 않는다.

3. **Base 정지 후 팔 배치**
   placement가 시작되면 mobile command는 exact zero이고, placement 중 mobile authority를 다시 열지 않는다.

4. **하나의 command owner**
   상자를 들고 있는 pallet runtime에서는 body와 mobile을 하나의 persistent combined stream에서 유지한다.

5. **Loaded hold 연속성**
   stream cancel은 단순 프로그램 종료가 아니라 상자 지지 명령의 종료일 수 있다. 첫 Ctrl-C는 hold/정지 handoff를 시도하고, 두 번째 강제 Ctrl-C는 상자를 떨어뜨릴 수 있다.

6. **F/T가 있다는 가정 금지**
   현재 pallet placement는 F/T를 사용하지 않는다. 접촉·seating·grip force가 증명됐다고 해석하지 않는다.

7. **Calibration 상태를 정직하게 표시**
   nominal registration을 validated calibration으로 이름만 바꾸지 않는다.

8. **실기 자동 동작은 감독형**
   replay/fake controller/test 통과는 물리 충돌 회피, 상자 지지, placement accuracy를 증명하지 않는다.

9. **출력 파일 덮어쓰기 금지**
   live MP4/JSONL은 매 실행 고유한 이름을 사용한다. 현재 CLI는 기존 파일 overwrite를 거부한다.

10. **물리 상수를 notes로만 두지 않기**
    상자 치수, stack 외곽, 중앙 홀, fixed camera/table 조건을 estimator와 gate의 실제 수식·config에 반영한다.

---

## 12. 완료 기준

### 12.1 Box Pick 완료 기준

현재 구현을 유지·개선할 때 최소 다음을 만족해야 한다.

- 대표 녹화와 live scene에서 상자 중심 x/y/z 및 장축 yaw overlay가 실제 상자 geometry와 안정적으로 일치한다.
- base가 `(0.740 m, 0.000 m)`와 가로 파지 yaw로 x/y/yaw를 동시에 수렴시킨다.
- 도착 후 exact zero와 wheel stop이 확인된다.
- grab-ready → impedance grab → lift가 상자 낙하 없이 끝난다.
- stale/lost/ambiguous vision에서는 base가 정지하고 grab을 시작하지 않는다.

현재는 위 항목이 가로 파지 데모 범위에서 대체로 확인됐지만, absolute calibration과 세로 파지는 남아 있다.

### 12.2 Slot-1 MVP 완료 기준

- 상자를 든 채 팔레트 정면에 대략 정렬된 상태에서 시작한다.
- slot-1 ready pose를 확인하거나 자동으로 이동한다.
- L-corner 또는 complete hole로 일관된 stack SE(2)를 얻는다.
- mobile base가 x/y/yaw를 동시에 수렴시키고 wheel stop까지 확인한다.
- 실제 fine `ARRIVED_HOLD`와 1.0 s dwell 전에는 lowering/release가 시작되지 않는다.
- frozen gap과 실제 높이가 일치하는 범위에서 충돌 없이 하강한다.
- base exact zero 상태에서 상자를 완전히 놓는다.
- 손을 안전하게 빼고 다음 상태로 후퇴한다.
- MP4와 JSONL로 각 gate, target, measured pose, failure reason을 재현할 수 있다.
- 동일 조건의 연속 성공 횟수 기준을 정하고 이를 만족한다.

현재는 alignment와 일부 placement code까지만 충족하며, retreat와 반복 실기 성공은 미완성이다.

### 12.3 전체 프로젝트 완료 기준

- 한 층의 slot 1~4를 순서대로 모두 적재한다.
- slide slot에서 갇힌 손이 안전하게 빠지고, 목표 상자만 이동하며, 이전 상자와 아래층이 밀리지 않는다.
- 다음 층에서 mirror된 인터락 방향이 동작한다.
- 배치 후 pose 검증과 오차 보정이 다음 slot에 반영된다.
- pick → carry → align → place → retreat가 하나의 반복 가능한 state machine과 stream ownership 계약으로 연결된다.
- timeout, vision loss, stream expiry, release 실패에서 안전하게 hold/abort/recover한다.
- 최종 위치 정확도와 연속 성공률의 수치 기준을 정의하고 실기로 충족한다.
- independent base calibration을 완료하거나, 최소한 사용자에게 승인받은 empirical tolerance와 한계를 명시한다.

---

## 13. 실행 진입점

명령은 각 subsystem 폴더에서 실행한다. 이 구조 마이그레이션은 하드웨어 없는
테스트와 replay로 검증됐으며, 물리 RB-Y1/D435 실기 검증을 수행했다는 뜻이 아니다.

### 13.1 Box Pick

GUI가 있는 Jetson desktop session:

```bash
cd Box_picking
python box_picking.py
```

원격/headless 실행 시에는 창만 끈다.

```bash
python box_picking.py --headless
```

기존 `--auto-grab`과 `--allow-nominal-registration`은 picking 명령에서 제거됐다.
자동 파지와 nominal registration은 `box_picking.py`의 기본 동작이며, 시작 시
nominal-unverified 경고를 계속 출력한다. `--config`, `--calibration`,
warmup/frame/window 옵션, robot address/power, `--output-mp4`, `--log-jsonl`은
진단용 expert 옵션으로 유지된다. MP4/JSONL은 경로를 명시한 경우에만 생성된다.

### 13.2 Slot-1 alignment-only

```bash
cd Box_placing
python box_pallet.py live \
  --headless \
  --ensure-slot1-ready \
  --auto-palletize-slot1 \
  --allow-nominal-registration \
  --allow-geometry-only-grip-check \
  --output-mp4 ../out/pallet_slot1_align_UNIQUE.mp4 \
  --log-jsonl ../out/pallet_slot1_align_UNIQUE.jsonl
```

### 13.3 Slot-1 alignment + supervised place

```bash
cd Box_placing
python box_pallet.py live \
  --headless \
  --ensure-slot1-ready \
  --auto-palletize-slot1 \
  --auto-place-slot1 \
  --allow-nominal-registration \
  --allow-geometry-only-grip-check \
  --allow-vision-geometry-release \
  --output-mp4 ../out/pallet_slot1_place_UNIQUE.mp4 \
  --log-jsonl ../out/pallet_slot1_place_UNIQUE.jsonl
```

`UNIQUE`는 실행 번호나 timestamp로 반드시 바꾼다.

### 13.4 녹화

현재 기록 진입점은 `Common/record.py`다.

```bash
cd /home/kgs/workspace/Palletizing
python Common/record.py --session-name SESSION_NAME --duration-sec 10
```

세부 옵션과 저장 위치는 `python Common/record.py --help` 및 루트 `RECORDING_GUIDE.md`를 따른다.

---

## 14. 주요 코드 위치

| 역할 | 위치 |
| --- | --- |
| Box-pick top-level | `Box_picking/box_picking.py` |
| Box pose estimator | `Box_picking/src/parcel_pose_picking/estimator.py` |
| Box mobile SE(2) servo | `Common/src/parcel_pose_common/mobile_servo.py` |
| Box grab/lift | `Box_picking/src/parcel_pose_picking/grabbing.py` |
| Box auto sequence | `Box_picking/src/parcel_pose_picking/auto_grab.py` |
| Pallet top-level | `Box_placing/box_pallet.py` |
| Pallet CLI | `Box_placing/src/parcel_pose_placing/pallet_cli.py` |
| Pallet geometry/perception | `Box_placing/src/parcel_pose_placing/pallet_geometry.py` |
| Pallet models/config parsing | `Box_placing/src/parcel_pose_placing/pallet_models.py` |
| Pallet fine servo | `Box_placing/src/parcel_pose_placing/pallet_servo.py` |
| Pallet placement FSM | `Box_placing/src/parcel_pose_placing/pallet_place.py` |
| RB-Y1 combined command/control | `Box_placing/src/parcel_pose_placing/pallet_control.py` |
| Live runtime orchestration | `Box_placing/src/parcel_pose_placing/pallet_runtime.py` |
| Slot-1 ready one-shot | `Box_placing/src/parcel_pose_placing/pallet_ready.py` |
| Box calibration/config | `Box_picking/configs/picking_config.json` |
| Slot-1 config | `Box_placing/configs/placing_config.json` |
| 공통 녹화 진입점 | `Common/record.py` |
| 공통 고정 pose config | `Common/configs/robot_fixed_pose_snapshot.json` |
| 기술 결정 기록 | `Box_placing/docs/adr/` 등 subsystem별 docs (배치 동작은 `0008-base-y-release-without-descent.md`가 최신) |
| 코드 슬림화 행동강령 | `docs/history/codex_slimming/CODE_SLIMMING.md` |
| 슬림화 작업 내역 | `docs/history/codex_slimming/SLIMMING_LOG.md` |
| 하드웨어 없는 회귀 테스트 | `Box_picking/tests/`, `Box_placing/tests/`, `Common/tests/` (`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1` 필요) |
| 루트 실행/녹화 요약 | `README.md`, `RECORDING_GUIDE.md` |

새 Agent는 구현 전에 반드시 현재 config와 해당 module을 다시 읽어야 한다. 이 문서는 목표와 계약의 기준이지만, runtime field 이름과 CLI 옵션의 최종 authority는 기준 커밋의 코드다.

---

## 15. 아직 필요한 실측과 미결정 사항

다음 값은 아직 정답이 없다. 임의의 “합리적인 기본값”으로 숨기지 말고, 로그 또는 실측으로 결정해야 한다.

- slot-1 최종 허용 오차: center x/y, yaw, 높이/seating
- slot-1 연속 성공 횟수와 전체 성공률 기준
- ~~실제 상자 바닥부터 stack top까지의 gap~~ → **실측 완료(2026-07-30)**:
  `159 mm` (5프레임 편차 ±0.2 mm, stack top `z = 0.4465 m`, 상자 바닥 `z = 0.6056 m`).
  `pallet_slot1_place_07.jsonl`의 `predicted_box_bottom_gap_m`에서 얻음
- **안전 하강량은 여전히 미결정**: `2/3 gap`(≈106 mm)은 특이점으로 실패했고
  현재 `15 mm`로 묶어 두었으나 실기 미검증이다. 상한을 올리며 특이점이 나는
  지점을 찾아야 한다. 실패 1회당 상자 낙하 + 강제 취소 비용이 있으므로
  완충재를 깔고 단계적으로 진행한다
- release에 필요한 최소 hand spread: 현재 손당 `30 mm`(상한 `40 mm`)로 설정했으나
  이 값으로 상자가 실제 이탈하는지 미검증. arm reach/singularity margin도 미실측
- slot-2/4 inner hand의 실제 swept volume과 필요한 offset `δ`
- 상자 적재 중량, 무게중심, 박스 간 마찰, 골판지 변형량
- one-hand push 속도, stiffness, yaw 허용오차
- slot-3 near reference와 slot-4 ring-closing clearance
- 최종 적재 층 수와 층별 z target
- F/T 없이 seating/release를 검증할 대체 기준
- independent base registration 검증 방법과 허용 오차
- marker orchestrator가 보장할 초기 위치/yaw 범위와 handoff interface
- 인접한 더 높은 stack이나 staging table이 stack-plane ROI에 들어오지 않도록 할 작업공간 ROI/제외 규칙
- fault/timeout 이후 operator recovery 절차

---

## 16. 새 Agent가 하지 말아야 할 것

- 현재 slot-1 MVP를 “전체 팔레타이징 완성”으로 오해하지 않는다.
- pure RGB bbox/Hough/scale search를 metric plane geometry 대신 기본 estimator로 되돌리지 않는다.
- 상자 실측 크기와 stack 실측 크기를 주석에만 남기고 알고리즘에서 무시하지 않는다.
- crop된 image border를 실제 상자나 stack edge로 취급하지 않는다.
- 관측되지 않은 자유도에 억지 confidence를 부여하지 않는다.
- `-4.2 mm` front-glass datum을 SDK depth에 더하지 않는다.
- 경험적 `+0.050 m` y correction을 ground truth로 승격하지 않는다.
- pallet body command와 mobile command를 별도 competing stream으로 나누지 않는다.
- placement 중 base motion authority를 다시 열지 않는다.
- F/T를 쓰지 않으면서 `SEATED`나 `contact confirmed`라고 주장하지 않는다.
- replay/test 성공만으로 physical collision safety와 placement accuracy가 검증됐다고 주장하지 않는다.
- 아직 쓰지 않는 일반화 계층과 future abstraction을 먼저 크게 만들지 않는다.
- 기존 동작을 보존할 수 있다면 새 abstraction보다 삭제, 직접적인 상태, 기존 utility를 우선한다.
- 이 문서의 8.6절 수치를 코드보다 우선하지 않는다. 배치 동작의 최종 권위는
  `Box_placing/docs/adr/0008-...`과 `Box_placing/configs/placing_config.json`이다.
- `SEATED`를 접촉 확인으로, 하강 `15 mm`를 안착으로 읽지 않는다. 상자는 약 `144 mm`
  높이에서 놓인다.
- `Box_picking/src/parcel_pose_picking/auto_grab.py` /
  `Box_picking/src/parcel_pose_picking/grabbing.py`를 미사용으로 판정해 제거하지 않는다.
  운영자가 계속 사용한다고 확인했고, `grabbing`은 `importlib.import_module`과
  `getattr` 문자열로 동적 호출되어 정적 참조가 0으로 보인다.
- subsystem별 회귀 테스트를 삭제하지 않는다. 이 저장소는 이미 한 번 163개 테스트를 잃었다.

---

## 17. 새로운 Agent에게 전달할 짧은 설명

다른 Agent에게는 다음과 같이 설명하면 된다.

> 우리의 목표는 RB-Y1 Model M v1.2가 D435 RGB-D를 이용해 고정된 400×253×160 mm급 상자를 찾아 양팔로 집고, 팔레트의 660×658 mm 핀휠 적층 구조에 네 상자씩 인터락 방식으로 반복 적재하도록 만드는 것이다. Box pick은 metric table/top-plane fitting, base x/y/yaw coupled servo, impedance grab/lift까지 구현되어 있다. Palletizing은 현재 상자를 든 채 3층 1번 먼쪽 가로 슬롯을 outer L-corner 또는 148×149 mm 중앙 홀로 인식하고, x/y/yaw를 동시에 맞춘 뒤 제한적으로 하강·release하는 supervised MVP만 구현되어 있으며 물리 커미셔닝 중이다. Slot 2와 4의 offset–inner-hand escape–one-hand slide, slot 3 direct place, 다음 층 mirror, retreat, pick-to-pallet atomic handoff는 아직 구현해야 한다. 카메라·torso·head·책상은 고정이고, base registration은 nominal이며 경험적 y +50 mm 보정만 있으므로 절대 calibration으로 간주하면 안 된다. F/T는 현재 pallet placement 판단에 사용하지 않으며, 관측 불확실·stream 문제·wheel 미정지 시에는 fail-closed해야 한다.
